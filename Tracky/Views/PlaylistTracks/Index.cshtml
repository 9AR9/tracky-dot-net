@model IEnumerable<Tracky.Domain.Entities.Music.PlaylistTrack>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Song.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlaylistId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlaylistTrackNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Song.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlaylistId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlaylistTrackNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
